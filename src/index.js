// eslint-disable-next-line no-unused-vars
import html from './index.html';
import './style.css';
import initNameModal from './UI/nameModal';
import PlayGame from './UI/DOM';

initNameModal();

// give out a prompt for a name / modal
// create such PlayGame class instance
// if restart is pressed then a new class instance
// is created but with same player name
